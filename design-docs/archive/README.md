# Archive

Earlier design work, kept for reference. Nothing in this folder is the current
direction. For that, start at [../README.md](../README.md).

| File | What it is | Still useful for |
|---|---|---|
| [Eddie_DESIGN.md](Eddie_DESIGN.md) | The first art, color and interaction reference, merged in #25. Uses a different palette (`azul`, `naranja`, `oro`, `verde`, `rosa`, `noche`) and different fonts (Archivo Black and Chivo) | Per-page art references (section 2) and per-page motion ideas (section 6.2) for pages other than the landing page. Its accessibility bar now lives in the style guide |
| `mockup_archive.pen` | Earlier pencil.dev landing page explorations: versions v1 to v5 and variants D1 to D5, going from an `ink`, `paper` and `rojo` palette to routed "Paracas border" and motherboard circuit motifs. That direction was dropped | Seeing what was already tried. Don't build from it |

Don't take tokens, fonts or layouts from anything in here without checking the current
docs first.
