import { HomeContent } from "@/app/_previewHome/HomeContent";

/** Option A: solid ink, sharp corners, gold top rule — everywhere. */
export default function PreviewA() {
  return <HomeContent cardVariant="ink" ctaVariant="ink" />;
}
