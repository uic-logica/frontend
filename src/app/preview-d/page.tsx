import { HomeContent } from "@/app/_previewHome/HomeContent";

/** Option D: torn-paper cutout edge — everywhere. */
export default function PreviewD() {
  return <HomeContent cardVariant="torn" ctaVariant="torn" />;
}
