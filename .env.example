# URL of the backend API (uic-logica/backend)
NEXT_PUBLIC_API_URL="http://localhost:3001"

# Where day-to-day club conversation happens. The dashboard links to it when
# set, and stays quiet when it isn't.
NEXT_PUBLIC_DISCORD_URL=""
